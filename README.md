# rescenedev Homebrew Tap

```sh
brew tap rescenedev/tap
brew install --cask lumen
```

Private tap: set a token if needed —
`export HOMEBREW_GITHUB_API_TOKEN=$(gh auth token)`

## Termina

Native macOS SSH & SFTP manager powered by libghostty.

```sh
brew install --cask rescenedev/tap/termina
```

Repo: https://github.com/rescenedev/termina
